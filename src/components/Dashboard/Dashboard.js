import React, { Component } from 'react';
import Product from '../Product/Product';

class Dashboard extends Component {
    render(props) {
        //  let listproducts = props.list.map((e, i) => {
        //     console.log(e);
        //     return (
        //          <div>
        //             // <Product key={i} product={e} />
        //         // </div>
        //      )
        //  })
         return (
             <div>
                {/* { {listproducts} } */}
             </div>
        )
    }
}

export default Dashboard;